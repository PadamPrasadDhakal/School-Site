
# Core
Django==5.2.4
gunicorn==21.2.0
whitenoise==6.5.0
python-dotenv==1.0.1

# NOTE: Django includes a sitemap framework. The external `django-sitemaps`
# package is unnecessary and can cause build problems on some build hosts
# (it was failing during wheel build on Render). If you need advanced
# sitemap helpers, add them later. For now we omit it to avoid build errors.

# Image handling (required for ImageField)
Pillow==10.0.1

# Database helpers (use Postgres in production; choose one)
# For psycopg2 (legacy adapter) use:
psycopg2-binary==2.9.9
# Or for psycopg v3 (modern) you can use: psycopg[binary]==3.2.2

# Convenience helper to parse DATABASE_URL (optional)
dj-database-url==1.2.0

# File storage on S3 (optional - enable if you use AWS S3 for media)
django-storages[boto3]==1.14.1
# Pin boto3 to a compatible released version
boto3==1.40.52

# Optional developer tools (only for dev)
django-debug-toolbar==4.1

# Optional: monitoring, caching, task queue (uncomment if used)
# sentry-sdk==1.21.0
# redis==5.0.0
# celery==5.3.0

# Cloudinary (optional) - add Cloudinary SDK and the Django storage wrapper if
# you plan to use Cloudinary for media storage. These are compatible with
# Python 3.11.x and Django 5.x.
cloudinary==1.44.1
django-cloudinary-storage==0.3.0

